{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C++ Launch (codelldb)",
      "type": "codelldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/main.exe",
      "cwd": "${workspaceFolder}"
    }
  ]
}
